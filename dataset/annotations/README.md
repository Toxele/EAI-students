# Разметка CVAT

Два экспорта из CVAT (формат Images 1.1):

| Папка | Что размечено | Статус |
|-------|---------------|--------|
| `task_2398159/` | Детальные ch1 (`000001`–`000067`, talc) | **51** из 80 кадров в job |
| `job_4195731/` | Плитки панорамы `4.jpg` (`001180`–`001403`) | **12** из 224 плиток (первые 12 тайлов) |

Метка в CVAT: **`talk`** (опечатка) → в датасете **`talc`**.

## Готовый результат

```
organized/
  masks/       # PNG-маски (белый = тальк)
  overlays/    # превью с полупрозрачной маской
  labels.csv   # все полигоны + связь с manifest
  summary.json # сводка по экспортам
  README.md
```

Пересобрать: `py scripts/import_cvat_annotations.py`

Связь с исходниками: `../cvat/manifest.csv` по колонке `cvat_filename`.
